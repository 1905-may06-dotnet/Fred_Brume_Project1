
using PizzaBox.Domain.Model;
using System.Collections.Generic;


namespace PizzaBox.Domain
{
    public class LocationHandler
    {
       public List<Plocation> GetLocationData()
        {
           //List<Plocation> plocation = new Crud().GetLocations();

           return null;     
        }

        public bool InsertLocation(Plocation location)
        {
            /*
            location.Street = street;
            location.City = city;
            location.PState = state;
            location.Zipcode = zipcode;

            int count = new Crud().AddLocation(location);

            if(count != 0)
            {
                return true;
            }

          

        */

            return false;
        }
    }
}

